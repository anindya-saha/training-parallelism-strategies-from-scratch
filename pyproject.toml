[project]
name = "training-parallelism-strategies-from-scratch"
version = "0.0.1"
description = "5D parallelism experiments"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "datasets>=4.0.0",
    "qwen_vl_utils==0.0.11",
    "torch==2.11.0",
    "torchaudio==2.11.0",
    "torchmetrics>=1.0.3",
    "torchvision==0.26.0",
    "torch-optimizer",
    "transformers[torch]",
    "deepspeed>=0.14.0",
    "omegaconf>=2.3.0",
    #"rich>=13.0.0",
    "torchtnt",
    "torchdata",
    "jinja2-cli>=0.8.0",
    "tensorboard",
    "torch-tb-profiler",
    "tiktoken",
    "datasets",
    "torchinfo",
    "ninja",
    "packaging",
    "flash-attn-4[cu13]>=4.0.0b1",
]

[project.optional-dependencies]
dev = [
    "black>=23.0.0",
    "isort>=5.12.0",
    "flake8>=6.0.0",
]

notebook = [
    "jupyter>=1.0.0",
    "notebook>=7.0.0",
    "ipykernel>=6.25.0",
    "ipywidgets>=8.0.0",
    "jupyterlab>=4.0.0",
    "nbdistributed==0.1.0",
    "matplotlib>=3.10.7",
    "seaborn>=0.13.2",
]

[tool.uv]
prerelease = "if-necessary-or-explicit"

[tool.uv.sources]
torch = [
  { index = "pytorch-cu130", marker = "sys_platform == 'linux'" },
]
torchvision = [
  { index = "pytorch-cu130", marker = "sys_platform == 'linux'" },
]
torchaudio = [
  { index = "pytorch-cu130", marker = "sys_platform == 'linux'" },
]
torchmetrics = [
  { index = "pytorch-cu130", marker = "sys_platform == 'linux'" },
]

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true


[tool.black]
line-length = 88
target-version = ['py312']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.venv
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"
line_length = 88
